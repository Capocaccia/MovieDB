{
  "name": "moviedb",
  "version": "1.0.0",
  "description": "movie database with firebase logins",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "./node_modules/.bin/hs -a localhost -p $((RANDOM + 10000)) -o",
    "build": "./build.sh",
    "start": "npm run build; npm run server",
    "deploy": "npm run build; ./deploy.sh",
    "watch": "./watch.sh; npm run server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Capocaccia/MovieDB.git"
  },
  "author": "Carter Capocacccia <carter.capocaccia@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Capocaccia/MovieDB/issues"
  },
  "homepage": "https://github.com/Capocaccia/MovieDB#readme",
  "devDependencies": {
    "babel": "^5.4.3",
    "http-server": "^0.8.0",
    "jade-cli": "^0.1.0",
    "node-sass": "^3.1.2",
    "nodemon": "^1.3.7"
  }
}
